# Environment Configuration
# Copy this file to .env and adjust values as needed

# Environment
NODE_ENV=development          # Options: development | production

# Logging
LOG_LEVEL=info               # Options: debug | info | warn | error
                             # Use 'debug' for development, 'info' for production

# Server
PORT=3000                    # Port to listen on

# Rate Limiting
RATE_LIMIT_MAX_REQUESTS=10   # Maximum requests per IP per time window
RATE_LIMIT_WINDOW_MS=60000   # Time window in milliseconds (60000 = 1 minute)
                             # Increase for development, keep low for production

# Caching
CACHE_TTL_MS=300000          # Cache time-to-live in milliseconds (300000 = 5 minutes)
                             # How long to keep cached crawl results
CACHE_MAX_SIZE=100           # Maximum number of entries in LRU cache
                             # Older entries are evicted when limit is reached
